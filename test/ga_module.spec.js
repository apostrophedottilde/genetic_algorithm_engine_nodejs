var assert = require('assert');
describe('ga module', () => {
  describe('crossover', () => {
    it('should swap a and b parts from each genome', () => {
      assert.true(2 === 1);
    });
  });
});
